document.querySelectorAll('.item-checkbox').forEach(checkbox => {
    checkbox.addEventListener('change', function() {
        // You could add code here to save the state to localStorage
        // or perform other actions when items are checked/unchecked
    });
});